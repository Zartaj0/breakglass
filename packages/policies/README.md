# packages/policies

Put incident trigger logic here:
- suspicious approval detection
- target allowlist checks
- value thresholds
- execution gating

These rules should anchor the system before any optional LLM explanation layer.
